/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.aceternity.com', 'images.unsplash.com', 'img.freepik.com'], // Add Freepik's domain here
    },
  };
  
  export default nextConfig;
  